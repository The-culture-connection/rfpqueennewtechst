{
  "name": "local-webhook-receiver",
  "version": "1.0.0",
  "description": "Local webhook receiver for testing webhook delivery",
  "main": "server.ts",
  "scripts": {
    "start": "ts-node server.ts",
    "dev": "ts-node --watch server.ts"
  },
  "dependencies": {
    "express": "^4.18.2",
    "@types/express": "^4.17.21"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}
